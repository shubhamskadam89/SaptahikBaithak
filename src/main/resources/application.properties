spring.application.name=assignment

spring.datasource.url=jdbc:postgresql://baithak-db-baithakassi.c.aivencloud.com:25325/defaultdb?ssl=require
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_FrR2dDp7cm-QZ3xK3ee
spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000


# ===============================
# Hibernate / JPA
# ===============================
spring.jpa.hibernate.ddl-auto=update
# Options: none | validate | update | create | create-drop
# -> create-drop = drop schema at startup, recreate, drop on shutdown

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# Dialect (Postgres)
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
